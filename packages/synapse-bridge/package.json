{
  "name": "@synapse/synapse-bridge",
  "version": "0.1.0",
  "description": "VS Code extension bridge for agent coordination and command routing.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "eslint \"src/**/*.{ts,tsx}\""
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
